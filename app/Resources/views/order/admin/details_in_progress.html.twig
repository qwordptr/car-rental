{% extends 'base.html.twig' %}
    {% block title %}Szczegóły zamówienia - pojazd u klienta{% endblock %}
    {% block body %}
        <div class="row details">
            <div class="col-lg-12">
                <div class="row">
                    <div class="col-lg-6">
                        <h3>Informacje o zamówieniu</h3>
                    </div>

                    <div class="col-lg-6">
                        <a href="#" class="btn btn-info">Pojazd wynajęty/u klienta</a>
                        <a href="{{ url("admin_order_finish_successfully", {id: order.id}) }}" class="btn btn-success">Zatwierdź zwrot</a>
                        <a href="{{ url("admin_order_finish_with_comments", {id: order.id}) }}" class="btn btn-warning">Zatwierdź zwrot z uwagami</a>
                    </div>
                </div>

                <div>
                    {{ include('order/admin/order-details.html.twig', { 'order': order }) }}
                </div>
            </div>
        </div>
    {% endblock %}